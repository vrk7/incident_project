# Copy this file to .env and customize before running the CLI.
# The assistant loads this file automatically (unless --skip-env-file is passed).
OPENAI_API_KEY=sk-your-key-here
# Optional: point to a non-default OpenAI-compatible endpoint
OPENAI_API_BASE=https://api.openai.com/v1
